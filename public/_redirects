# SPA 폴백.
#
# 이 사이트는 클라이언트 사이드 라우팅(React Router)을 씁니다.
# 그래서 /brows, /smp, /academy 같은 주소에 해당하는 파일이 서버에 없습니다.
# 이 규칙이 없으면 손님이 그 주소로 직접 들어오거나 새로고침할 때 404 가 납니다.
#
# 실제 파일(=sitemap.xml, robots.txt, /images/*, /assets/*)이 있으면
# 그 파일이 먼저 나가고, 없을 때만 index.html 을 200 으로 돌려줍니다.
#
# Netlify 와 Cloudflare Pages 둘 다 이 파일을 인식합니다.

/*    /index.html   200
